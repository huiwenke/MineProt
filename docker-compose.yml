version: '3'
services:
  nginx:
    restart: always
    image: nginx:stable-alpine
    ports:
      - 80:80
    volumes:
      - ./web:/var/www/html
      - ./config:/etc/nginx/conf.d
      - ./data:/var/www/data
    links:
      - es:elasticsearch
      - php:MinePHP
  php:
    restart: always
    build: ./app/php
    volumes:
      - ./web:/var/www/html
  es:
    build: ./app/elasticsearch
    restart: always
    ports:
      - 9233:9200
      - 9300:9300
