version: '3'
services:
  nginx:
    container_name: MineNginx
    restart: always
    image: nginx:stable-alpine
    ports:
      - 80:80
    volumes:
      - ./web:/var/www/html
      - ./data:/var/www/data
      - ./config:/etc/nginx/conf.d
    links:
      - es:elasticsearch
      - php:MinePHP
  php:
    restart: always
    build: ./app/php
    volumes:
      - ./web:/var/www/html
      - ./data:/var/www/data
    links:
      - es:elasticsearch
  es:
    build: ./app/elasticsearch
    restart: always
    ports:
      - 9233:9200
      - 9300:9300